<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.share.goods.mapper.ProductSkuMapper">

    <resultMap type="com.share.goods.domain.ProductSku" id="ProductSkuResult" autoMapping="true">
    </resultMap>

    <sql id="selectSkuVo">
        select id, product_id, merchant_id, specs, price, original_price, stock, image,
               sales, status, ext_json, create_by, create_time, update_by, update_time, del_flag
        from product_sku
    </sql>

    <select id="selectSkuList" parameterType="com.share.goods.domain.ProductSku" resultMap="ProductSkuResult">
        <include refid="selectSkuVo"/>
        <where>
            <if test="productId != null"> and product_id = #{productId}</if>
            <if test="merchantId != null"> and merchant_id = #{merchantId}</if>
            <if test="status != null and status != ''"> and status = #{status}</if>
            and del_flag = 0
        </where>
        order by price asc
    </select>

</mapper>

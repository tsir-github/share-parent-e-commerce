<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.share.order.mapper.OrderInfoMapper">

    <!-- 乐观锁更新订单状态（含当前状态校验，防并发覆盖） -->
    <update id="updateOrderStatus">
        UPDATE order_info
        SET status = #{status},
            version = version + 1,
            update_time = NOW()
        WHERE id = #{id}
          AND version = #{version}
        <if test="expectedStatus != null and expectedStatus != ''">
          AND status = #{expectedStatus}
        </if>
    </update>

    <!-- 按日期范围统计订单数量（安全） -->
    <select id="getOrderCountByDate" resultType="java.util.Map">
        SELECT DATE_FORMAT(create_time, '%Y-%m-%d') AS order_date,
               COUNT(*) AS order_count
        FROM order_info
        WHERE del_flag = '0'
          <if test="startDate != null and startDate != ''">
              AND create_time >= #{startDate}
          </if>
          <if test="endDate != null and endDate != ''">
              AND create_time &lt;= CONCAT(#{endDate}, ' 23:59:59')
          </if>
        GROUP BY order_date
        ORDER BY order_date
    </select>

    <!-- @deprecated SQL 注入风险 — 使用 getOrderCountByDate -->
    <select id="getOrderCount" parameterType="string" resultType="java.util.Map">
        ${value}
    </select>

</mapper>

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.share.order.mapper.OrderReviewMapper">

    <!-- 批量查询商品评价统计 -->
    <select id="selectReviewStatsByProductIds" resultType="java.util.Map">
        SELECT oi.product_id AS productId, ROUND(AVG(r.rating), 1) AS avgRating, COUNT(*) AS reviewCount
        FROM order_review r
        JOIN order_item oi ON r.order_item_id = oi.id
        WHERE oi.product_id IN
        <foreach collection="productIds" item="pid" open="(" separator="," close=")">
            #{pid}
        </foreach>
        AND r.del_flag = '0' AND r.status = '0'
        GROUP BY oi.product_id
    </select>

    <!-- 查询单个商品评价统计 -->
    <select id="selectReviewStatsByProductId" resultType="java.util.Map">
        SELECT ROUND(AVG(rating), 1) AS avgRating, COUNT(*) AS reviewCount
        FROM order_review r
        JOIN order_item oi ON r.order_item_id = oi.id
        WHERE oi.product_id = #{productId}
        AND r.del_flag = '0' AND r.status = '0'
    </select>

</mapper>

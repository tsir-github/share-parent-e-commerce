<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.share</groupId>
        <artifactId>share-modules</artifactId>
        <version>3.6.3</version>
    </parent>
    <artifactId>share-order</artifactId>

    <dependencies>
        <!-- SpringCloud Alibaba Nacos -->
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-discovery</artifactId>
        </dependency>

        <!-- SpringCloud Alibaba Nacos Config -->
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-config</artifactId>
        </dependency>

        <!-- SpringCloud Alibaba Sentinel -->
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-sentinel</artifactId>
        </dependency>

        <!-- Sentinel Datasource Nacos -->
        <dependency>
            <groupId>com.alibaba.csp</groupId>
            <artifactId>sentinel-datasource-nacos</artifactId>
        </dependency>

        <!-- SpringBoot Actuator -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- Mysql Connector -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
        </dependency>

        <!-- RuoYi Common DataScope -->
        <dependency>
            <groupId>com.share</groupId>
            <artifactId>share-common-datascope</artifactId>
        </dependency>

        <!-- RuoYi Common Log -->
        <dependency>
            <groupId>com.share</groupId>
            <artifactId>share-common-log</artifactId>
        </dependency>

        <!-- RocketMQ 消息队列 -->
        <dependency>
            <groupId>org.apache.rocketmq</groupId>
            <artifactId>rocketmq-spring-boot-starter</artifactId>
        </dependency>

        <!-- Seata 分布式事务 — 
             ponytail: share-order 无 @GlobalTransactional 调用，移除依赖避免本地 @Transactional 被 Seata 拦截。 
             需要分布式事务时取消注释。 -->
        <!-- dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-seata</artifactId>
        </dependency -->

        <!-- Redisson 分布式锁 (Redis Sentinel) -->
        <dependency>
            <groupId>com.share</groupId>
            <artifactId>share-common-redis</artifactId>
        </dependency>

        <!-- 订单 Feign 接口（含 Domain DTO） -->
        <dependency>
            <groupId>com.share</groupId>
            <artifactId>share-api-order</artifactId>
        </dependency>

        <!-- 支付 Feign 接口（含 PaymentSuccessMessage DTO） -->
        <dependency>
            <groupId>com.share</groupId>
            <artifactId>share-api-payment</artifactId>
        </dependency>

        <!-- 商品 Feign 接口（含 ProductSku, StockDeductDTO） -->
        <dependency>
            <groupId>com.share</groupId>
            <artifactId>share-api-goods</artifactId>
        </dependency>

        <!-- 优惠券 Feign 接口（含 RemoteCouponService） -->
        <dependency>
            <groupId>com.share</groupId>
            <artifactId>share-api-coupon</artifactId>
        </dependency>

        <!-- 用户 Feign 接口（含 RemoteUserService） -->
        <dependency>
            <groupId>com.share</groupId>
            <artifactId>share-api-user</artifactId>
        </dependency>

    </dependencies>

</project>